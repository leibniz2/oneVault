<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/add_picture"
        android:icon="@mipmap/icon_add_file"
        android:title="@string/appbar_tools"
        app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_clear"
        android:title="@string/appbar_clear"
        app:showAsAction="never" />

    <item android:id="@+id/action_transfer"
        android:title="@string/appbar_transfer"
        app:showAsAction="never" />

    <item android:id="@+id/action_delete"
        android:title="@string/appbar_delete"
        app:showAsAction="never"/>

</menu>